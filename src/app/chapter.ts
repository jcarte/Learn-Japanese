export class Chapter{
    chapterId: number;
    description: string;
    constructor(chapterId: number, description: string) { 
        this.chapterId = chapterId;
        this.description =description;
    }
}